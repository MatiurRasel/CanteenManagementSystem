@{
    ViewData["Title"] = "ক্যান্টিন অর্ডার সিস্টেম";
    Layout = null;
}

<!DOCTYPE html>
<html lang="bn">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <style>
        @@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+Bengali:wght@400;500;600;700;800;900&display=swap');

        * {
            font-family: 'Noto Sans Bengali', sans-serif;
            -webkit-tap-highlight-color: transparent;
        }

        :root {
            --primary-color: #4F46E5;
            --secondary-color: #10B981;
            --accent-color: #F59E0B;
            --background-color: #F8FAFC;
            --card-bg: #FFFFFF;
            --text-primary: #1E293B;
            --text-secondary: #64748B;
            --border-color: #E2E8F0;
            --success-color: #10B981;
            --warning-color: #F59E0B;
            --error-color: #EF4444;
        }

        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            overflow-x: hidden;
            touch-action: manipulation;
            transition: background 0.3s ease;
        }

        .glass-effect {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            box-shadow: 0 8px 32px rgba(31, 38, 135, 0.15);
            border: 1px solid rgba(255, 255, 255, 0.3);
            color: var(--text-primary);
            transition: all 0.3s ease;
        }

        .scan-animation {
            animation: scan 2s ease-in-out infinite;
        }

        @@keyframes scan {
            0%, 100% {
                transform: translateY(-10px) scale(1.05);
                opacity: 0.7;
            }

            50% {
                transform: translateY(10px) scale(1.1);
                opacity: 1;
            }
        }

        .pulse-ring {
            animation: pulse-ring 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
        }

        @@keyframes pulse-ring {
            0% {
                transform: scale(1);
                opacity: 1;
            }

            100% {
                transform: scale(1.8);
                opacity: 0;
            }
        }

        .slide-in-bottom {
            animation: slideInBottom 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        @@keyframes slideInBottom {
            from {
                transform: translateY(30px) scale(0.95);
                opacity: 0;
            }

            to {
                transform: translateY(0) scale(1);
                opacity: 1;
            }
        }

        .fade-in {
            animation: fadeIn 0.4s ease-out;
        }

        @@keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.9);
            }

            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        .bounce-in {
            animation: bounceIn 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        }

        @@keyframes bounceIn {
            0% {
                transform: scale(0.3);
                opacity: 0;
            }

            50% {
                transform: scale(1.05);
            }

            70% {
                transform: scale(0.9);
            }

            100% {
                transform: scale(1);
                opacity: 1;
            }
        }

        input {
            color: var(--text-primary);
            background: white;
            transition: all 0.3s ease;
            border: 2px solid var(--border-color);
        }

            input:focus {
                outline: none;
                border-color: var(--primary-color);
                box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.2);
            }

        .menu-item {
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
            background: white;
            color: var(--text-primary);
            border: 1px solid var(--border-color);
        }

            .menu-item::before {
                content: '';
                position: absolute;
                top: 0;
                left: -100%;
                width: 100%;
                height: 100%;
                background: linear-gradient(90deg, transparent, rgba(79, 70, 229, 0.1), transparent);
                transition: 0.5s;
            }

            .menu-item:hover::before {
                left: 100%;
            }

            .menu-item:hover {
                transform: translateY(-8px) scale(1.02);
                box-shadow: 0 20px 40px rgba(79, 70, 229, 0.15);
                border-color: var(--primary-color);
            }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color) 0%, #7C3AED 100%);
            color: white;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
            border: none;
        }

            .btn-primary::before {
                content: '';
                position: absolute;
                top: 50%;
                left: 50%;
                width: 0;
                height: 0;
                border-radius: 50%;
                background: rgba(255, 255, 255, 0.2);
                transform: translate(-50%, -50%);
                transition: width 0.6s, height 0.6s;
            }

            .btn-primary:hover::before {
                width: 300px;
                height: 300px;
            }

            .btn-primary:hover {
                transform: translateY(-3px) scale(1.02);
                box-shadow: 0 15px 30px rgba(79, 70, 229, 0.3);
            }

            .btn-primary:active {
                transform: translateY(-1px) scale(0.98);
            }

        .quantity-btn {
            transition: all 0.2s ease;
            touch-action: manipulation;
        }

            .quantity-btn:active {
                transform: scale(0.9);
            }

        /* Enhanced Scrollbar Styling */
        .custom-scrollbar {
            scrollbar-width: thin;
            scrollbar-color: var(--primary-color) #F1F5F9;
        }

            .custom-scrollbar::-webkit-scrollbar {
                width: 8px;
                height: 8px;
            }

            .custom-scrollbar::-webkit-scrollbar-track {
                background: #F1F5F9;
                border-radius: 8px;
            }

            .custom-scrollbar::-webkit-scrollbar-thumb {
                background: linear-gradient(135deg, var(--primary-color) 0%, #7C3AED 100%);
                border-radius: 8px;
                border: 2px solid #F1F5F9;
            }

                .custom-scrollbar::-webkit-scrollbar-thumb:hover {
                    background: linear-gradient(135deg, #4338CA 0%, #6D28D9 100%);
                }

        /* Responsive Grid */
        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 1.5rem;
        }

        @@media (max-width: 768px) {
            .menu-grid {
                grid-template-columns: 1fr;
            }
        }

        /* Card Styling */
        .card-hover {
            transition: all 0.3s ease;
            cursor: pointer;
        }

            .card-hover:hover {
                transform: translateY(-5px);
                box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            }

        /* Loading Skeleton */
        .skeleton {
            background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
            background-size: 200% 100%;
            animation: loading 1.5s infinite;
        }

        @@keyframes loading {
            0% {
                background-position: 200% 0;
            }

            100% {
                background-position: -200% 0;
            }
        }

        /* Badge Animations */
        .badge-pulse {
            animation: badgePulse 2s infinite;
        }

        @@keyframes badgePulse {
            0%, 100% {
                box-shadow: 0 0 0 0 rgba(79, 70, 229, 0.4);
            }

            50% {
                box-shadow: 0 0 0 8px rgba(79, 70, 229, 0);
            }
        }

        /* EdTech Themed Elements */
        .edtech-gradient {
            background: linear-gradient(135deg, #4F46E5 0%, #10B981 100%);
        }

        .edtech-gradient-secondary {
            background: linear-gradient(135deg, #F59E0B 0%, #F97316 100%);
        }

        .edtech-gradient-success {
            background: linear-gradient(135deg, #10B981 0%, #059669 100%);
        }

        /* Mobile Optimizations */
        @@media (max-width: 640px) {
            .mobile-stack {
                flex-direction: column;
            }

            .mobile-full {
                width: 100% !important;
                max-width: 100% !important;
            }

            .mobile-text-center {
                text-align: center;
            }

            .mobile-padding {
                padding: 1rem !important;
            }

            .mobile-gap {
                gap: 0.75rem !important;
            }
        }

        /* Touch Device Optimizations */
        @@media (hover: none) and (pointer: coarse) {
            .menu-item:hover {
                transform: none;
            }

            .btn-primary:hover {
                transform: none;
            }

            .quantity-btn {
                min-height: 44px;
                min-width: 44px;
            }
        }

        /* Print Styles */
        @@media print {
            body {
                background: white !important;
            }

            .glass-effect {
                box-shadow: none !important;
                border: 1px solid #ddd !important;
            }
        }

        /* Accessibility */
        .focus-visible:focus-visible {
            outline: 3px solid var(--primary-color);
            outline-offset: 2px;
        }

        /* Reduced Motion */
        @@media (prefers-reduced-motion: reduce) {
            * {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
        }

        /* Manual input mode */
        .manual-input-active {
            animation: manualInputGlow 1.5s ease-in-out infinite alternate;
            border-color: var(--primary-color);
        }

        @@keyframes manualInputGlow {
            from {
                box-shadow: 0 0 5px rgba(79, 70, 229, 0.3);
            }

            to {
                box-shadow: 0 0 15px rgba(79, 70, 229, 0.6);
            }
        }

        /* EdTech Icons Styling */
        .edtech-icon {
            background: linear-gradient(135deg, var(--primary-color), #7C3AED);
            color: white;
            border-radius: 12px;
            padding: 12px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 12px rgba(79, 70, 229, 0.2);
        }

        /* Progress Bar */
        .progress-bar {
            height: 8px;
            border-radius: 4px;
            overflow: hidden;
            background: #E2E8F0;
        }

            .progress-bar .progress-fill {
                height: 100%;
                background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
                transition: width 1s ease;
            }

        /* Status Indicators */
        .status-online {
            background: var(--success-color);
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: 600;
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.2);
        }

        /* Modern Card Design */
        .modern-card {
            background: white;
            border-radius: 16px;
            padding: 24px;
            box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
            border: 1px solid var(--border-color);
            transition: all 0.3s ease;
        }

            .modern-card:hover {
                box-shadow: 0 8px 32px rgba(0, 0, 0, 0.12);
                transform: translateY(-2px);
            }
    </style>
</head>
<body class="antialiased">
    <!-- VERIFICATION SCREEN -->
    <div id="verificationScreen" class="min-h-screen flex items-center justify-center p-4 md:p-8">
        <div class="glass-effect rounded-3xl p-6 md:p-12 w-full max-w-2xl mx-4 modern-card">
            <!-- Logo/Header -->
            <div class="mb-6 md:mb-8 text-center">
                <div class="relative inline-block mb-4 md:mb-6">
                    <div class="absolute inset-0 pulse-ring bg-indigo-400 rounded-full"></div>
                    <div class="relative edtech-gradient w-20 h-20 md:w-24 md:h-24 rounded-full flex items-center justify-center mx-auto shadow-2xl">
                        <i class="fas fa-graduation-cap text-3xl md:text-4xl text-white"></i>
                    </div>
                </div>
                <h1 class="text-3xl md:text-5xl font-black text-gray-900 mb-2 md:mb-3 leading-tight">
                    ক্যান্টিন অর্ডার সিস্টেম
                </h1>
                <p class="text-lg md:text-xl text-gray-700 font-medium">চট্টগ্রাম ক্যান্টনমেন্ট পাবলিক কলেজ</p>
                <p class="text-base md:text-lg text-gray-600 mt-1 md:mt-2">স্মার্ট কার্ড স্ক্যান করে অর্ডার করুন</p>
            </div>

            <!-- Scan Icon -->
            <div class="my-8 md:my-12 scan-animation">
                <div class="edtech-icon inline-block p-6 rounded-2xl">
                    <i class="fas fa-id-card text-5xl md:text-6xl text-white"></i>
                </div>
            </div>

            <!-- Instructions -->
            <div class="mb-6 md:mb-8 text-center">
                <h2 class="text-2xl md:text-3xl font-bold text-gray-900 mb-3 md:mb-4">আপনার আইডি কার্ড স্ক্যান করুন</h2>
                <p class="text-base md:text-lg text-gray-700">
                    <i class="fas fa-qrcode mr-2 text-indigo-500"></i>
                    স্ক্যানার, QR কোড, বা বারকোড ব্যবহার করুন
                </p>
            </div>

            <!-- Input Field -->
            <div class="mb-6 md:mb-8">
                <div class="relative">
                    <input type="text"
                           id="cardInput"
                           class="w-full px-6 py-5 md:px-8 md:py-6 text-2xl md:text-3xl text-center border-3 border-gray-300 rounded-2xl focus:border-indigo-500 transition-all duration-300 shadow-lg focus:shadow-xl bg-white"
                           placeholder="আপনার আইডি এখানে লিখুন..."
                           autocomplete="off"
                           aria-label="আপনার আইডি লিখুন"
                           readonly />
                    <div class="absolute right-4 top-1/2 transform -translate-y-1/2">
                        <i class="fas fa-id-card text-2xl text-gray-400"></i>
                    </div>
                </div>
                <p class="text-sm text-gray-500 mt-2 text-center">
                    <i class="fas fa-info-circle mr-1 text-indigo-500"></i>
                    ম্যানুয়ালি আইডি লিখতে নিচের বাটনে ক্লিক করুন
                </p>
            </div>

            <!-- Error Message -->
            <div id="errorMessage" class="hidden mb-6 bg-gradient-to-r from-red-50 to-orange-50 border-l-4 border-red-500 p-6 rounded-xl shadow-lg">
                <div class="flex items-center">
                    <i class="fas fa-exclamation-triangle text-2xl text-red-500 mr-3"></i>
                    <p class="text-red-700 font-semibold text-lg" id="errorText"></p>
                </div>
            </div>

            <!-- Loading -->
            <div id="loadingSpinner" class="hidden">
                <div class="flex flex-col items-center justify-center py-6">
                    <div class="relative">
                        <div class="animate-spin rounded-full h-20 w-20 border-t-4 border-b-4 border-indigo-500"></div>
                        <div class="absolute inset-0 animate-ping rounded-full border-4 border-indigo-300 opacity-75"></div>
                    </div>
                    <p class="text-gray-700 mt-6 text-lg font-medium">যাচাই করা হচ্ছে...</p>
                    <p class="text-gray-500 text-sm mt-2">অনুগ্রহ করে অপেক্ষা করুন</p>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="flex flex-col md:flex-row gap-4 mt-8">
                <button onclick="enableManualInput()"
                        id="manualInputBtn"
                        class="btn-primary text-white px-8 py-4 rounded-xl font-bold text-lg md:text-xl flex-1 flex items-center justify-center gap-3 shadow-xl">
                    <i class="fas fa-keyboard text-xl"></i>
                    ম্যানুয়ালি আইডি লিখুন
                </button>
                @* <button onclick="scanWithCamera()"
                        class="edtech-gradient-secondary text-white px-8 py-4 rounded-xl font-bold text-lg md:text-xl flex-1 flex items-center justify-center gap-3 shadow-xl hover:shadow-2xl transition-all">
                    <i class="fas fa-camera text-xl"></i>
                    ক্যামেরা স্ক্যান
                </button> *@
            </div>

            <!-- Help Text -->
            <div class="mt-8 text-center">
                <p class="text-gray-600 text-sm">
                    <i class="fas fa-question-circle mr-2 text-indigo-500"></i>
                    সমস্যা হলে অফিসে যোগাযোগ করুন
                </p>
            </div>
        </div>
    </div>

    <!-- ORDER SCREEN (Hidden Initially) -->
    <div id="orderScreen" class="hidden min-h-screen p-4 md:p-8">
        <div class="max-w-7xl mx-auto">
            <!-- Header with User Info -->
            <div class="glass-effect rounded-3xl p-6 md:p-8 mb-6 slide-in-bottom modern-card">
                <div class="flex flex-col md:flex-row md:items-center justify-between gap-6">
                    <div class="flex items-center gap-4 md:gap-6">
                        <div id="userPhotoContainer" class="relative">
                            <img id="userPhoto" src="" alt="User Photo"
                                 class="w-16 h-16 md:w-20 md:h-20 rounded-2xl object-cover border-4 border-white shadow-2xl"
                                 onerror="this.onerror=null; this.src='data:image/svg+xml;base64,'">
                            <div class="absolute -bottom-2 -right-2 edtech-gradient text-white rounded-full w-7 h-7 md:w-8 md:h-8 flex items-center justify-center shadow-lg">
                                <i id="userTypeIcon" class="fas fa-user text-xs md:text-sm"></i>
                            </div>
                        </div>
                        <div class="flex-1">
                            <h2 class="text-2xl md:text-3xl font-black text-gray-900 truncate" id="userName">Loading...</h2>
                            <p class="text-base md:text-lg text-gray-700 truncate" id="userInfo">Loading...</p>
                            <div class="flex flex-wrap items-center gap-2 mt-2">
                                <span id="userTypeBadge" class="px-3 py-1.5 bg-indigo-100 text-indigo-800 rounded-full font-bold text-sm badge-pulse"></span>
                                <span id="userIdBadge" class="px-3 py-1.5 bg-blue-100 text-blue-800 rounded-full font-bold text-sm"></span>
                            </div>
                        </div>
                    </div>
                    <div class="text-center md:text-right">
                        <p class="text-sm text-gray-700 mb-3 font-medium">
                            <i class="fas fa-wallet mr-2 text-indigo-500"></i>
                            মাসিক ব্যালেন্স তথ্য
                        </p>
                        <div class="grid grid-cols-3 gap-3 md:gap-6">
                            <div class="text-center bg-gradient-to-b from-indigo-50 to-white p-3 md:p-4 rounded-xl shadow-md">
                                <p class="text-xs md:text-sm text-gray-600 mb-1">মোট ব্যালেন্স</p>
                                <p class="text-xl md:text-2xl font-black text-gray-900">৳<span id="totalBalance">0</span></p>
                            </div>
                            <div class="text-center bg-gradient-to-b from-orange-50 to-white p-3 md:p-4 rounded-xl shadow-md">
                                <p class="text-xs md:text-sm text-gray-600 mb-1">ব্যবহৃত</p>
                                <p class="text-xl md:text-2xl font-black text-orange-600">৳<span id="usedBalance">0</span></p>
                            </div>
                            <div class="text-center bg-gradient-to-b from-green-50 to-white p-3 md:p-4 rounded-xl shadow-md">
                                <p class="text-xs md:text-sm text-gray-600 mb-1">উপলব্ধ</p>
                                <p class="text-2xl md:text-3xl font-black text-green-600">৳<span id="availableBalance">0</span></p>
                            </div>
                        </div>
                        <p class="text-xs text-gray-500 mt-3">
                            <i class="far fa-calendar-alt mr-1 text-indigo-500"></i>
                            আজকের তারিখ: <span id="currentDate" class="font-semibold text-gray-700"></span>
                        </p>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                <!-- Menu Items -->
                <div class="lg:col-span-2">
                    <div class="glass-effect rounded-3xl p-6 md:p-8 slide-in-bottom modern-card">
                        <div class="flex flex-col md:flex-row md:items-center justify-between mb-6 gap-4">
                            <h3 class="text-2xl md:text-3xl font-bold text-gray-900 flex items-center gap-3">
                                <div class="edtech-icon w-10 h-10">
                                    <i class="fas fa-book-open text-white"></i>
                                </div>
                                আজকের মেনু
                            </h3>
                            <div class="flex items-center gap-4">
                                <div class="relative">
                                    <input type="text"
                                           id="menuSearch"
                                           placeholder="খাবার খুঁজুন..."
                                           class="pl-10 pr-4 py-2.5 border-2 border-gray-300 rounded-xl focus:border-indigo-500 focus:outline-none w-full md:w-64 transition-all bg-white text-gray-900">
                                    <i class="fas fa-search absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                                </div>
                                <span class="edtech-gradient text-white px-4 py-2.5 rounded-full font-bold shadow-lg">
                                    <span id="menuItemCount">0</span> টি আইটেম
                                </span>
                            </div>
                        </div>

                        <div id="menuItems" class="menu-grid custom-scrollbar max-h-[calc(100vh-300px)] overflow-y-auto pr-2">
                            <!-- Menu items will be loaded here -->
                            <div class="skeleton h-48 rounded-2xl"></div>
                            <div class="skeleton h-48 rounded-2xl"></div>
                            <div class="skeleton h-48 rounded-2xl"></div>
                            <div class="skeleton h-48 rounded-2xl"></div>
                        </div>

                        <div class="mt-6 pt-6 border-t border-gray-200">
                            <p class="text-gray-600 text-sm text-center">
                                <i class="fas fa-info-circle mr-2 text-indigo-500"></i>
                                স্ক্রল করে সব মেনু দেখুন
                            </p>
                        </div>
                    </div>
                </div>

                <!-- Cart -->
                <div class="lg:col-span-1">
                    <div class="glass-effect rounded-3xl p-6 md:p-8 sticky top-6 slide-in-bottom modern-card">
                        <h3 class="text-2xl md:text-3xl font-bold text-gray-900 mb-6 flex items-center gap-3">
                            <div class="edtech-icon w-10 h-10">
                                <i class="fas fa-shopping-cart text-white"></i>
                            </div>
                            আপনার অর্ডার
                        </h3>

                        <div id="cartItems" class="custom-scrollbar space-y-4 mb-6 min-h-[200px] max-h-[400px] overflow-y-auto p-2">
                            <div class="text-center py-12 text-gray-400">
                                <div class="inline-block p-6 bg-gradient-to-br from-gray-100 to-gray-50 rounded-2xl mb-4">
                                    <i class="fas fa-shopping-basket text-5xl text-gray-300"></i>
                                </div>
                                <p class="text-xl font-medium text-gray-500">কোনো আইটেম নির্বাচিত নয়</p>
                                <p class="text-sm text-gray-400 mt-2">মেনু থেকে আইটেম নির্বাচন করুন</p>
                            </div>
                        </div>

                        <div class="border-t-2 border-gray-200 pt-6 mb-6 space-y-4">
                            <div class="flex justify-between items-center">
                                <span class="text-lg text-gray-700 font-medium">মোট আইটেম:</span>
                                <span class="text-xl font-black text-gray-900" id="cartItemCount">0</span>
                            </div>
                            <div class="flex justify-between items-center text-2xl font-black mb-2">
                                <span class="text-gray-900">সর্বমোট:</span>
                                <span class="text-transparent bg-clip-text bg-gradient-to-r from-indigo-600 to-purple-600">
                                    ৳<span id="cartTotal">0</span>
                                </span>
                            </div>
                            <div class="bg-gradient-to-r from-yellow-50 to-orange-50 p-4 rounded-xl border border-yellow-200">
                                <p class="text-sm text-gray-700 flex items-center gap-2">
                                    <i class="fas fa-wallet text-yellow-500"></i>
                                    অর্ডার দেয়ার পর ব্যালেন্স থাকবে:
                                    <span class="font-black text-green-600 ml-auto">৳<span id="remainingAfterOrder">0</span></span>
                                </p>
                            </div>
                        </div>

                        <div class="space-y-4">
                            <button onclick="placeOrder()"
                                    id="placeOrderBtn"
                                    disabled
                                    class="w-full btn-primary text-white px-8 py-5 rounded-2xl font-black text-xl disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none shadow-2xl hover:shadow-3xl transition-all duration-300 flex items-center justify-center gap-3">
                                <i class="fas fa-check-circle text-xl"></i>
                                অর্ডার নিশ্চিত করুন
                            </button>

                            <div class="grid grid-cols-2 gap-3">
                                <button onclick="clearCart()"
                                        class="flex-1 bg-gradient-to-r from-gray-200 to-gray-300 text-gray-800 px-6 py-3.5 rounded-xl font-bold hover:from-gray-300 hover:to-gray-400 transition-all shadow-md flex items-center justify-center gap-2">
                                    <i class="fas fa-trash-alt"></i>
                                    পরিষ্কার করুন
                                </button>
                                <button onclick="cancelOrder()"
                                        class="flex-1 edtech-gradient-secondary text-white px-6 py-3.5 rounded-xl font-bold hover:opacity-90 transition-all shadow-md flex items-center justify-center gap-2">
                                    <i class="fas fa-times"></i>
                                    বাতিল করুন
                                </button>
                            </div>

                            <button onclick="showOrderHistory()"
                                    class="w-full bg-gradient-to-r from-blue-50 to-cyan-50 text-blue-700 px-6 py-3.5 rounded-xl font-bold border-2 border-blue-200 hover:border-blue-300 transition-all shadow-sm flex items-center justify-center gap-2">
                                <i class="fas fa-history text-blue-500"></i>
                                পূর্বের অর্ডার দেখুন
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- SUCCESS SCREEN -->
    <div id="successScreen" class="hidden min-h-screen flex items-center justify-center p-4 md:p-8">
        <div class="glass-effect rounded-3xl p-8 md:p-12 w-full max-w-2xl mx-4 fade-in modern-card">
            <div class="text-center">
                <div class="relative inline-block mb-8">
                    <div class="absolute inset-0 animate-ping bg-green-400 rounded-full opacity-75"></div>
                    <div class="relative edtech-gradient-success w-32 h-32 rounded-full flex items-center justify-center mx-auto shadow-2xl">
                        <i class="fas fa-graduation-cap text-7xl text-white"></i>
                    </div>
                </div>

                <h1 class="text-3xl md:text-5xl font-black text-gray-900 mb-4">অর্ডার সফল হয়েছে!</h1>
                <p class="text-xl md:text-2xl text-gray-700 mb-8 font-medium">
                    <i class="fas fa-utensils mr-2 text-indigo-500"></i>
                    আপনার খাবার প্রস্তুত করা হচ্ছে
                </p>

                <div class="bg-gradient-to-br from-indigo-50 to-purple-50 rounded-2xl p-6 md:p-8 mb-8 border border-indigo-200 shadow-lg">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                        <div class="text-center p-4 bg-white rounded-xl shadow-sm">
                            <p class="text-gray-600 mb-2 font-medium">
                                <i class="fas fa-hashtag mr-2 text-indigo-500"></i>
                                অর্ডার নম্বর
                            </p>
                            <p class="text-3xl md:text-4xl font-black text-indigo-600" id="successOrderNumber">---</p>
                        </div>
                        <div class="text-center p-4 bg-white rounded-xl shadow-sm">
                            <p class="text-gray-600 mb-2 font-medium">
                                <i class="fas fa-money-bill-wave mr-2 text-indigo-500"></i>
                                মোট পরিমাণ
                            </p>
                            <p class="text-3xl md:text-4xl font-black text-indigo-600">৳<span id="successAmount">0</span></p>
                        </div>
                    </div>

                    <div class="bg-gradient-to-r from-green-50 to-emerald-50 p-6 rounded-xl border border-green-200">
                        <p class="text-gray-700 mb-3 font-bold text-lg flex items-center justify-center gap-2">
                            <i class="fas fa-wallet text-green-500"></i>
                            অবশিষ্ট ব্যালেন্স
                        </p>
                        <p class="text-4xl md:text-5xl font-black text-green-600">৳<span id="successRemainingBalance">0</span></p>
                    </div>
                </div>

                <div class="text-gray-600 mb-8 space-y-4">
                    <p class="text-lg bg-gradient-to-r from-blue-50 to-cyan-50 p-4 rounded-xl border border-blue-200">
                        <i class="fas fa-info-circle mr-2 text-blue-500"></i>
                        অনুগ্রহ করে অর্ডার নম্বরটি মনে রাখুন
                    </p>
                    <div class="bg-gradient-to-r from-yellow-50 to-orange-50 p-4 rounded-xl border border-yellow-200">
                        <p class="font-medium">
                            <i class="fas fa-clock mr-2 text-orange-500"></i>
                            স্বয়ংক্রিয়ভাবে রিসেট হচ্ছে
                            <span id="countdown" class="font-black text-indigo-600 text-2xl mx-2">10</span>
                            সেকেন্ডে...
                        </p>
                        <div class="progress-bar mt-3">
                            <div id="countdownBar" class="progress-fill" style="width: 100%"></div>
                        </div>
                    </div>
                </div>

                <div class="flex flex-col md:flex-row gap-4">
                    <button onclick="printReceipt()"
                            class="btn-primary text-white px-8 py-4 rounded-xl font-bold text-lg flex-1 flex items-center justify-center gap-3 shadow-xl">
                        <i class="fas fa-print"></i>
                        রিসিপ্ট প্রিন্ট করুন
                    </button>
                    <button onclick="resetToVerification()"
                            class="edtech-gradient-secondary text-white px-8 py-4 rounded-xl font-bold text-lg flex-1 flex items-center justify-center gap-3 shadow-xl hover:shadow-2xl transition-all">
                        <i class="fas fa-home"></i>
                        প্রথম পৃষ্ঠায় ফিরে যান
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Floating Action Button -->
    <button id="backToTop"
            class="hidden fixed bottom-6 right-6 w-14 h-14 edtech-gradient text-white rounded-full flex items-center justify-center shadow-2xl hover:shadow-3xl transition-all z-50"
            onclick="scrollToTop()">
        <i class="fas fa-arrow-up text-xl"></i>
    </button>

    <!-- Connection Status Indicator -->
    <div id="connectionStatus" class="fixed top-4 right-4 z-50">
        <div class="status-online flex items-center gap-2">
            <div class="w-2 h-2 bg-white rounded-full animate-pulse"></div>
            <span>অনলাইন</span>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script>
        // Global variables
        let currentUser = null;
        let cart = {};
        let menuData = [];
        let orderTimeout = null;
        let countdownInterval = null;
        let searchTimeout = null;
        let isManualInputEnabled = false;

        // Initialize
        $(document).ready(function() {
            initializeApp();
            setupEventListeners();
            checkConnection();
        });

        function initializeApp() {
            $('#cardInput').focus();
            displayCurrentDate();
            setupSwipeGestures();

            // Set initial input state
            $('#cardInput').prop('readonly', true);
        }

        function setupEventListeners() {
            // Auto-submit on Enter (only when manual input is enabled)
            $('#cardInput').on('keypress', function(e) {
                if (e.which === 13 && !$(this).prop('readonly')) {
                    verifyUser();
                }
            });

            // Auto-submit for card scanners (always active)
            $('#cardInput').on('input', function() {
                if ($(this).prop('readonly')) return;

                clearTimeout(window.cardScanTimeout);
                window.cardScanTimeout = setTimeout(() => {
                    if ($(this).val().length >= 3) {
                        verifyUser();
                    }
                }, 500);
            });

            // Menu search with debounce
            $('#menuSearch').on('input', function() {
                clearTimeout(searchTimeout);
                searchTimeout = setTimeout(() => {
                    filterMenuItems($(this).val());
                }, 300);
            });

            // Back to top button
            $(window).on('scroll', function() {
                if ($(this).scrollTop() > 100) {
                    $('#backToTop').fadeIn();
                } else {
                    $('#backToTop').fadeOut();
                }
            });

            // Prevent form submission on enter in search
            $('#menuSearch').on('keypress', function(e) {
                if (e.which === 13) {
                    e.preventDefault();
                }
            });

            // Click outside to disable manual input
            $(document).on('click', function(e) {
                if (!$(e.target).closest('#cardInput, #manualInputBtn').length && isManualInputEnabled) {
                    disableManualInput();
                }
            });

            // Escape key to disable manual input
            $(document).on('keydown', function(e) {
                if (e.key === 'Escape' && isManualInputEnabled) {
                    disableManualInput();
                }
            });
        }

        function setupSwipeGestures() {
            let touchStartX = 0;
            let touchStartY = 0;
            let touchEndX = 0;
            let touchEndY = 0;

            document.addEventListener('touchstart', function(event) {
                touchStartX = event.changedTouches[0].screenX;
                touchStartY = event.changedTouches[0].screenY;
            }, false);

            document.addEventListener('touchend', function(event) {
                touchEndX = event.changedTouches[0].screenX;
                touchEndY = event.changedTouches[0].screenY;
                handleSwipe();
            }, false);

            function handleSwipe() {
                const deltaX = touchEndX - touchStartX;
                const deltaY = touchEndY - touchStartY;

                // Minimum swipe distance
                if (Math.abs(deltaX) < 50 && Math.abs(deltaY) < 50) return;

                // Horizontal swipe (left/right)
                if (Math.abs(deltaX) > Math.abs(deltaY)) {
                    if (deltaX > 0) {
                        // Swipe right - go back
                        if ($('#orderScreen').is(':visible')) {
                            cancelOrder();
                        }
                    }
                }
            }
        }

        function checkConnection() {
            const statusElement = $('#connectionStatus');
            if (navigator.onLine) {
                statusElement.html(`
                    <div class="status-online flex items-center gap-2">
                        <div class="w-2 h-2 bg-white rounded-full animate-pulse"></div>
                        <span>অনলাইন</span>
                    </div>
                `);
            } else {
                statusElement.html(`
                    <div class="bg-gradient-to-r from-red-500 to-pink-600 text-white px-4 py-2 rounded-full shadow-lg flex items-center gap-2">
                        <div class="w-2 h-2 bg-white rounded-full"></div>
                        <span>অফলাইন</span>
                    </div>
                `);
            }
        }

        function displayCurrentDate() {
            const now = new Date();
            const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
            const dateString = now.toLocaleDateString('bn-BD', options);
            $('#currentDate').text(dateString);
        }

        function enableManualInput() {
            isManualInputEnabled = true;
            const input = $('#cardInput');
            input.prop('readonly', false);
            input.focus().select();
            input.addClass('manual-input-active');
            $('#manualInputBtn').addClass('bg-gradient-to-r from-green-500 to-emerald-500');
            showToast('আইডি লিখতে প্রস্তুত', 'info');
        }

        function disableManualInput() {
            isManualInputEnabled = false;
            const input = $('#cardInput');
            input.prop('readonly', true);
            input.removeClass('manual-input-active');
            $('#manualInputBtn').removeClass('bg-gradient-to-r from-green-500 to-emerald-500');
        }

        function scanWithCamera() {
            showToast('ক্যামেরা স্ক্যান শীঘ্রই আসছে...', 'info');
        }

        async function verifyUser() {
            const identifier = $('#cardInput').val().trim();

            if (!identifier) {
                showError('অনুগ্রহ করে আপনার কার্ড স্ক্যান করুন বা আইডি লিখুন');
                return;
            }

            // Clear previous user data and cart
            currentUser = null;
            cart = {};
            menuData = [];

            // Reset all screens to default state
            $('#successScreen').addClass('hidden');
            $('#orderScreen').addClass('hidden');
            $('#verificationScreen').removeClass('hidden');

            hideError();
            $('#loadingSpinner').removeClass('hidden');
            $('#cardInput').prop('disabled', true);

            try {
                const response = await fetch('/Verification/VerifyUser', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ identifier })
                });

                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }

                const data = await response.json();

                if (data.success) {
                    currentUser = data;
                    menuData = data.todayMenu || [];

                    // Hide loading spinner FIRST
                    $('#loadingSpinner').addClass('hidden');
                    $('#cardInput').prop('disabled', false);

                    // Then show order screen
                    showOrderScreen();
                    disableManualInput();
                } else {
                    showError(data.message);
                    $('#loadingSpinner').addClass('hidden');
                    $('#cardInput').val('').prop('disabled', false).focus();
                    if (isManualInputEnabled) {
                        $('#cardInput').prop('readonly', false);
                    }
                }
            } catch (error) {
                console.error('Verification error:', error);
                showError('সংযোগে সমস্যা হয়েছে। আবার চেষ্টা করুন।');
                $('#loadingSpinner').addClass('hidden');
                $('#cardInput').prop('disabled', false).focus();
                if (isManualInputEnabled) {
                    $('#cardInput').prop('readonly', false);
                }
            }
        }

        function showOrderScreen() {
            // Make sure loading spinner is hidden
            $('#loadingSpinner').addClass('hidden');

            // Hide verification screen and show order screen
            $('#verificationScreen').addClass('hidden');
            $('#orderScreen').removeClass('hidden');

            // Scroll to top
            $('html, body').animate({ scrollTop: 0 }, 300);

            // Reset cart display
            $('#cartItems').html(`
                <div class="text-center py-12">
                    <div class="inline-block p-6 bg-gradient-to-br from-gray-100 to-gray-50 rounded-2xl mb-4">
                        <i class="fas fa-shopping-basket text-5xl text-gray-300"></i>
                    </div>
                    <p class="text-xl font-medium text-gray-500">কোনো আইটেম নির্বাচিত নয়</p>
                    <p class="text-sm text-gray-400 mt-2">মেনু থেকে আইটেম নির্বাচন করুন</p>
                </div>
            `);

            // Reset order button state
            $('#placeOrderBtn').prop('disabled', true)
                .html('<i class="fas fa-check-circle mr-3"></i>অর্ডার নিশ্চিত করুন');

            // Reset quantity displays
            $('.menu-item span[id^="qty-"]').text('0');

            // Populate user info
            $('#userName').text(currentUser.userName || 'Unnamed User');
            $('#userInfo').text(currentUser.academicInformation || 'No additional information');
            $('#userIdBadge').text(currentUser.userIdentifier || currentUser.userId || identifier);

            // Set user type
            const userType = currentUser.userType;
            let userTypeText = '';
            let userIcon = 'fas fa-user';

            if (userType === 1) { // Student
                userTypeText = 'শিক্ষার্থী';
                userIcon = 'fas fa-user-graduate';
                $('#userTypeIcon').removeClass().addClass(userIcon);
            } else if (userType === 2) { // Employee

               const employeeTypeName = currentUser.employeeTypeName || 'টিচার/স্টাফ';
               userTypeText = employeeTypeName.toUpperCase();
                userIcon = 'fas fa-user-tie';
                $('#userTypeIcon').removeClass().addClass(userIcon);
            }
            console.log(userTypeText)
            $('#userTypeBadge').text(userTypeText);

            // Set user photo with fallback
            const userPhoto = $('#userPhoto');
            if (currentUser.userPhotoUrl) {
                userPhoto.attr('src', currentUser.userPhotoUrl);
                userPhoto.on('error', function() {
                    generateAvatar(currentUser.userName, userType);
                });
            } else {
                generateAvatar(currentUser.userName, userType);
            }

            // Set balance info
            $('#totalBalance').text(formatCurrency(currentUser.totalBalance || 0));
            $('#usedBalance').text(formatCurrency(currentUser.usedBalance || 0));
            $('#availableBalance').text(formatCurrency(currentUser.availableBalance || 0));

            // Populate menu
            displayMenu();

            // Clear cart totals
            $('#cartTotal').text('0');
            $('#cartItemCount').text('0');
            $('#remainingAfterOrder').text(formatCurrency(currentUser.availableBalance || 0));

            // Start session timeout
            startSessionTimeout();
        }

        function generateAvatar(name, userType) {
            const colors = userType === 1 ? ['4F46E5', '7C3AED'] : ['10B981', '059669'];
            const nameParts = (name || 'User').split(' ');
            const initials = nameParts.map(p => p.charAt(0)).join('').toUpperCase().substring(0, 2);
            $('#userPhoto').attr('src',
                `https://ui-avatars.com/api/?name=${encodeURIComponent(initials)}&background=${colors[0]}&color=fff&size=80&bold=true&font-size=0.4`
            );
        }

        function formatCurrency(amount) {
            return parseFloat(amount).toFixed(2).replace(/\B(?=(\d{3})+(?!\d))/g, ",");
        }

        function displayMenu() {
            const container = $('#menuItems');
            container.empty();

            if (!menuData || menuData.length === 0) {
                container.html(`
                    <div class="col-span-full text-center py-16">
                        <div class="inline-block p-8 bg-gradient-to-br from-gray-100 to-gray-50 rounded-3xl mb-6">
                            <i class="fas fa-utensils text-6xl text-gray-300"></i>
                        </div>
                        <h3 class="text-2xl font-bold text-gray-500 mb-3">আজকের জন্য কোনো মেনু নেই</h3>
                        <p class="text-gray-400">অপারেটরকে জানান মেনু যোগ করার জন্য</p>
                    </div>
                `);
                $('#menuItemCount').text(0);
                return;
            }

            $('#menuItemCount').text(menuData.length);

            menuData.forEach((item, index) => {
                const isOutOfStock = !item.inStock || item.availableQuantity <= 0;
                const discount = item.discount || 0;
                const originalPrice = discount > 0 ? item.price / (1 - discount/100) : item.price;

                container.append(`
                    <div class="menu-item rounded-2xl p-5 hover:shadow-xl transition-all duration-300"
                         data-name="${item.itemName.toLowerCase()}"
                         data-category="${item.category}">
                        <div class="flex items-start justify-between mb-4">
                            <div class="flex-1">
                                <div class="flex items-start justify-between mb-2">
                                    <h4 class="text-xl font-black text-gray-900 truncate pr-2">${item.itemName}</h4>
                                    ${discount > 0 ? `
                                        <span class="bg-gradient-to-r from-red-500 to-pink-500 text-white text-xs font-bold px-2 py-1 rounded-full">
                                            ${discount}% ছাড়
                                        </span>
                                    ` : ''}
                                </div>
                                <p class="text-sm text-gray-600 mb-3 line-clamp-2 min-h-[2.5rem]">${item.description}</p>
                                <div class="flex flex-wrap items-center gap-2">
                                    <span class="px-3 py-1 ${getCategoryColor(item.category)} text-gray-900 rounded-full text-xs font-bold">
                                        ${getCategoryText(item.category)}
                                    </span>
                                    <span class="text-xs text-gray-500">
                                        <i class="fas fa-box mr-1 text-indigo-400"></i>
                                        উপলব্ধ: <span class="font-bold">${item.availableQuantity}</span> টি
                                    </span>
                                </div>
                            </div>
                            <span class="text-4xl ml-3">${getEmojiForCategory(item.category)}</span>
                        </div>

                        <div class="flex items-center justify-between mt-4">
                            <div class="flex flex-col">
                                ${discount > 0 ? `
                                    <span class="text-sm text-gray-400 line-through">৳${originalPrice.toFixed(2)}</span>
                                ` : ''}
                                <span class="text-2xl font-black text-transparent bg-clip-text bg-gradient-to-r from-indigo-600 to-purple-600">
                                    ৳${item.price.toFixed(2)}
                                </span>
                            </div>

                            ${!isOutOfStock ? `
                                <div class="flex items-center gap-2">
                                    <button onclick="changeQuantity(${item.dailyMenuID}, ${item.foodItemID}, -1, event)"
                                            class="quantity-btn bg-gradient-to-r from-gray-200 to-gray-300 w-10 h-10 rounded-full hover:from-gray-300 hover:to-gray-400 transition-all shadow-sm">
                                        <i class="fas fa-minus text-gray-700"></i>
                                    </button>
                                    <span id="qty-${item.dailyMenuID}" class="w-12 text-center font-black text-xl text-gray-900">0</span>
                                    <button onclick="changeQuantity(${item.dailyMenuID}, ${item.foodItemID}, 1, ${item.availableQuantity}, event)"
                                            class="quantity-btn btn-primary text-white w-10 h-10 rounded-full shadow-md">
                                        <i class="fas fa-plus text-sm"></i>
                                    </button>
                                </div>
                            ` : `
                                <span class="bg-gradient-to-r from-red-100 to-pink-100 text-red-700 px-4 py-2 rounded-full text-sm font-bold border border-red-200">
                                    <i class="fas fa-times mr-1"></i>
                                    শেষ হয়ে গেছে
                                </span>
                            `}
                        </div>
                    </div>
                `);
            });
        }

        function filterMenuItems(searchTerm) {
            const term = searchTerm.toLowerCase().trim();
            $('.menu-item').each(function() {
                const itemName = $(this).data('name');
                const shouldShow = term === '' || itemName.includes(term);
                $(this).toggle(shouldShow);
            });

            const visibleCount = $('.menu-item:visible').length;
            $('#menuItemCount').text(visibleCount);
        }

        function getCategoryText(category) {
            switch(category) {
                case 1: return 'ব্রেকফাস্ট';
                case 2: return 'লাঞ্চ';
                case 3: return 'স্ন্যাকস';
                case 4: return 'ড্রিংকস';
                case 5: return 'ডিনার';
                default: return 'অন্যান্য';
            }
        }

        function getCategoryColor(category) {
            switch(category) {
                case 1: return 'bg-gradient-to-r from-yellow-500 to-amber-600';
                case 2: return 'bg-gradient-to-r from-green-500 to-emerald-600';
                case 3: return 'bg-gradient-to-r from-pink-500 to-rose-600';
                case 4: return 'bg-gradient-to-r from-blue-500 to-cyan-600';
                case 5: return 'bg-gradient-to-r from-purple-500 to-violet-600';
                default: return 'bg-gradient-to-r from-gray-500 to-gray-800';
            }
        }

        function getEmojiForCategory(category) {
            switch(category) {
                case 1: return '🍳';
                case 2: return '🍚';
                case 3: return '🍔';
                case 4: return '🥤';
                case 5: return '🍽️';
                default: return '🍴';
            }
        }

        function changeQuantity(menuId, foodItemId, change, maxQty, event) {
            if (event) {
                event.stopPropagation();
                // Add click feedback
                $(event.currentTarget).addClass('scale-90');
                setTimeout(() => {
                    $(event.currentTarget).removeClass('scale-90');
                }, 150);
            }

            if (!cart[menuId]) {
                cart[menuId] = { menuId, foodItemId, quantity: 0 };
            }

            cart[menuId].quantity += change;

            if (cart[menuId].quantity < 0) cart[menuId].quantity = 0;
            if (maxQty && cart[menuId].quantity > maxQty) {
                cart[menuId].quantity = maxQty;
                showError(`সর্বোচ্চ ${maxQty} টি অর্ডার করতে পারবেন`);
            }

            if (cart[menuId].quantity === 0) {
                delete cart[menuId];
            }

            // Animate quantity change
            const qtyElement = $(`#qty-${menuId}`);
            qtyElement.addClass('scale-110');
            setTimeout(() => qtyElement.removeClass('scale-110'), 150);
            qtyElement.text(cart[menuId]?.quantity || 0);

            updateCart();
        }

        function updateCart() {
            const container = $('#cartItems');
            let total = 0;
            let itemCount = 0;
            let html = '';

            for (let menuId in cart) {
                const item = menuData.find(m => m.dailyMenuID == menuId);
                if (item && cart[menuId].quantity > 0) {
                    const itemTotal = item.price * cart[menuId].quantity;
                    total += itemTotal;
                    itemCount += cart[menuId].quantity;

                    html += `
                        <div class="bg-gradient-to-r from-white to-indigo-50 p-4 rounded-xl border border-indigo-100 shadow-sm hover:shadow-md transition-all">
                            <div class="flex justify-between items-start mb-2">
                                <div class="flex-1">
                                    <p class="font-black text-gray-900 text-lg truncate">${item.itemName}</p>
                                    <div class="flex justify-between items-center mt-3">
                                        <div class="flex items-center gap-2">
                                            <button onclick="changeQuantity(${menuId}, ${item.foodItemID}, -1, null, event)"
                                                    class="quantity-btn w-8 h-8 bg-gray-200 rounded-full flex items-center justify-center hover:bg-gray-300">
                                                <i class="fas fa-minus text-xs text-gray-700"></i>
                                            </button>
                                            <span class="font-bold text-gray-900 w-8 text-center">${cart[menuId].quantity}</span>
                                            <button onclick="changeQuantity(${menuId}, ${item.foodItemID}, 1, ${item.availableQuantity}, event)"
                                                    class="quantity-btn w-8 h-8 edtech-gradient rounded-full flex items-center justify-center hover:opacity-90">
                                                <i class="fas fa-plus text-xs text-white"></i>
                                            </button>
                                        </div>
                                        <p class="font-black text-transparent bg-clip-text bg-gradient-to-r from-indigo-600 to-purple-600">
                                            ৳${itemTotal.toFixed(2)}
                                        </p>
                                    </div>
                                    <p class="text-xs text-gray-500 mt-2">
                                        <i class="fas fa-tag mr-1 text-indigo-400"></i>
                                        ${cart[menuId].quantity} × ৳${item.price.toFixed(2)}
                                    </p>
                                </div>
                                <button onclick="removeFromCart(${menuId})"
                                        class="ml-4 text-red-500 hover:text-red-700 transition-colors p-2">
                                    <i class="fas fa-times"></i>
                                </button>
                            </div>
                        </div>
                    `;
                }
            }

            if (html === '') {
                html = `
                    <div class="text-center py-12">
                        <div class="inline-block p-6 bg-gradient-to-br from-gray-100 to-gray-50 rounded-2xl mb-4">
                            <i class="fas fa-shopping-basket text-5xl text-gray-300"></i>
                        </div>
                        <p class="text-xl font-medium text-gray-500">কোনো আইটেম নির্বাচিত নয়</p>
                        <p class="text-sm text-gray-400 mt-2">মেনু থেকে আইটেম নির্বাচন করুন</p>
                    </div>
                `;
                $('#placeOrderBtn').prop('disabled', true);
            } else {
                $('#placeOrderBtn').prop('disabled', false);
            }

            container.html(html);
            $('#cartTotal').text(formatCurrency(total));
            $('#cartItemCount').text(itemCount);
            $('#remainingAfterOrder').text(formatCurrency(currentUser.availableBalance - total));

            // Animate cart update
            container.addClass('scale-105');
            setTimeout(() => container.removeClass('scale-105'), 150);
        }

        function removeFromCart(menuId) {
            delete cart[menuId];
            $(`#qty-${menuId}`).text('0');
            updateCart();
            showToast('আইটেম কার্ট থেকে সরানো হয়েছে', 'info');
        }

        function clearCart() {
            if (Object.keys(cart).length === 0) return;

            if (confirm('আপনি কি নিশ্চিত যে সব আইটেম মুছে ফেলতে চান?')) {
                cart = {};
                $('.menu-item span[id^="qty-"]').text('0');
                updateCart();
                showToast('কার্ট পরিষ্কার করা হয়েছে', 'success');
            }
        }

        async function placeOrder() {
            const items = [];
            for (let menuId in cart) {
                if (cart[menuId].quantity > 0) {
                    items.push({
                        dailyMenuId: parseInt(menuId),
                        foodItemId: cart[menuId].foodItemId,
                        quantity: cart[menuId].quantity
                    });
                }
            }

            if (items.length === 0) {
                showError('অনুগ্রহ করে কমপক্ষে একটি আইটেম নির্বাচন করুন');
                return;
            }

            // Check if balance is sufficient
            const cartTotal = parseFloat($('#cartTotal').text().replace(/,/g, ''));
            if (currentUser.availableBalance < cartTotal) {
                showError('আপনার পর্যাপ্ত ব্যালেন্স নেই।');
                return;
            }

            $('#placeOrderBtn').prop('disabled', true)
                .html('<i class="fas fa-spinner fa-spin mr-3"></i>প্রক্রিয়াকরণ হচ্ছে...');

            try {
                const response = await fetch('/Order/PlaceOrder', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        userId: currentUser.userId,
                        userIdentifier: currentUser.userIdentifier,
                        userType: currentUser.userType,
                        items: items
                    })
                });

                const data = await response.json();

                if (data.success) {
                    showSuccessScreen(data);
                } else {
                    showError(data.message || 'অর্ডার দিতে সমস্যা হয়েছে।');
                    $('#placeOrderBtn').prop('disabled', false)
                        .html('<i class="fas fa-check-circle mr-3"></i>অর্ডার নিশ্চিত করুন');
                }
            } catch (error) {
                console.error('Order error:', error);
                showError('অর্ডার প্রক্রিয়াকরণে সমস্যা হয়েছে। আবার চেষ্টা করুন।');
                $('#placeOrderBtn').prop('disabled', false)
                    .html('<i class="fas fa-check-circle mr-3"></i>অর্ডার নিশ্চিত করুন');
            }
        }

        function showSuccessScreen(data) {
            clearTimeout(orderTimeout);
            if (countdownInterval) clearInterval(countdownInterval);

            $('#orderScreen').addClass('hidden');
            $('#successScreen').removeClass('hidden');
            $('html, body').animate({ scrollTop: 0 }, 300);

            $('#successOrderNumber').text(data.orderNumber || '---');
            $('#successAmount').text(formatCurrency(data.totalAmount || 0));
            $('#successRemainingBalance').text(formatCurrency(data.remainingBalance || 0));

            // Countdown with progress bar
            let count = 10;
            $('#countdown').text(count);
            const countdownBar = $('#countdownBar');
            countdownBar.css('width', '100%');

            countdownInterval = setInterval(() => {
                count--;
                $('#countdown').text(count);
                const progress = (count / 10) * 100;
                countdownBar.css('width', `${progress}%`);

                if (count <= 0) {
                    clearInterval(countdownInterval);
                    resetToVerification();
                }
            }, 1000);
        }

        function printReceipt() {
            const printContent = `
                <div style="font-family: 'Noto Sans Bengali', sans-serif; padding: 20px;">
                    <h1 style="text-align: center; color: #4F46E5;">অর্ডার রিসিপ্ট</h1>
                    <p>অর্ডার নম্বর: ${$('#successOrderNumber').text()}</p>
                    <p>তারিখ: ${new Date().toLocaleDateString('bn-BD')}</p>
                    <p>মোট পরিমাণ: ৳${$('#successAmount').text()}</p>
                    <hr>
                    <p style="text-align: center;">ধন্যবাদ</p>
                </div>
            `;

            const printWindow = window.open('', '_blank');
            printWindow.document.write(printContent);
            printWindow.document.close();
            printWindow.print();
        }

        function showOrderHistory() {
            alert('এই ফিচারটি শীঘ্রই আসছে...');
        }

        function cancelOrder() {
            if (confirm('আপনি কি নিশ্চিত যে অর্ডার বাতিল করতে চান? সব নির্বাচিত আইটেম মুছে যাবে।')) {
                resetToVerification();
            }
        }

        function resetToVerification() {
            clearTimeout(orderTimeout);
            if (countdownInterval) clearInterval(countdownInterval);

            currentUser = null;
            cart = {};
            menuData = [];
            isManualInputEnabled = false;

            // Reset all screens
            $('#successScreen').addClass('hidden');
            $('#orderScreen').addClass('hidden');
            $('#verificationScreen').removeClass('hidden');

            // Reset input field
            const input = $('#cardInput');
            input.val('').prop('disabled', false).prop('readonly', true);
            input.removeClass('manual-input-active');
            $('#manualInputBtn').removeClass('bg-gradient-to-r from-green-500 to-emerald-500');
            $('#menuSearch').val('');

            // Hide loading spinner just in case
            $('#loadingSpinner').addClass('hidden');

            // Focus on input
            input.focus();

            hideError();
        }

        function startSessionTimeout() {
            clearTimeout(orderTimeout);
            orderTimeout = setTimeout(() => {
                showError('সেশন টাইমআউট (২ মিনিট)। অনুগ্রহ করে আবার শুরু করুন।');
                setTimeout(() => resetToVerification(), 3000);
            }, 2 * 60 * 1000); // 2 minutes
        }

        function scrollToTop() {
            $('html, body').animate({ scrollTop: 0 }, 500);
        }

        function showError(message) {
            $('#errorText').text(message);
            $('#errorMessage').removeClass('hidden').addClass('bounce-in');
            setTimeout(hideError, 5000);
        }

        function hideError() {
            $('#errorMessage').removeClass('bounce-in').addClass('hidden');
        }

        function showToast(message, type = 'info') {
            const colors = {
                success: 'bg-gradient-to-r from-green-500 to-emerald-600',
                error: 'bg-gradient-to-r from-red-500 to-pink-600',
                warning: 'bg-gradient-to-r from-yellow-500 to-amber-600',
                info: 'edtech-gradient'
            };

            const toast = $(`
                <div class="fixed top-4 left-1/2 transform -translate-x-1/2 ${colors[type]} text-white px-6 py-3 rounded-full shadow-2xl z-50 animate-slideInDown">
                    <div class="flex items-center gap-3">
                        <i class="fas fa-${type === 'success' ? 'check-circle' : type === 'error' ? 'exclamation-circle' : 'info-circle'}"></i>
                        <span class="font-medium">${message}</span>
                    </div>
                </div>
            `);

            $('body').append(toast);
            setTimeout(() => {
                toast.animate({ opacity: 0, top: '-100px' }, 300, function() {
                    $(this).remove();
                });
            }, 3000);
        }

        // Add CSS for slideInDown animation
        const style = document.createElement('style');
        style.textContent = `
        @@keyframes slideInDown {
                from { transform: translate(-50%, -100px); opacity: 0; }
                to { transform: translate(-50%, 0); opacity: 1; }
            }
        `;
        document.head.appendChild(style);

        // Handle offline/online events
        window.addEventListener('online', checkConnection);
        window.addEventListener('offline', checkConnection);
    </script>
</body>
</html>