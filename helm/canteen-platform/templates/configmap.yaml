apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "canteen.fullname" . }}-env
  labels:
    {{- include "canteen.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
