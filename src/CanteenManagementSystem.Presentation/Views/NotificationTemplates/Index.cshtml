@*
    /admin/notification-templates — per-tenant template editor.
*@
@using CanteenManagementSystem.Presentation.Models
@model IReadOnlyList<CanteenManagementSystem.Presentation.Controllers.NotificationTemplatesController.TemplateRowVm>
@inject IStringLocalizer<SharedResource> L
@{
    ViewData["Title"] = L["NotificationTemplates"].Value;
    var header = new UiPageHeaderOptions
    {
        Title    = L["NotificationTemplates"].Value,
        Subtitle = L["NotificationTemplates.Index.Subtitle"].Value
    };
}

<div class="container-fluid py-4">
    <partial name="_UiPageHeader" model="header" />

    @if (Model.Count == 0)
    {
        var info = new UiAlertOptions
        {
            Variant = UiAlertVariant.Info,
            Heading = L["NotificationTemplates.Index.EmptyTitle"].Value,
            Body    = L["NotificationTemplates.Index.EmptyBody"].Value
        };
        <partial name="_UiAlert" model="info" />
    }
    else
    {
        <div class="ui-stack">
        @foreach (var t in Model)
        {
            <form method="post" asp-action="Edit" asp-route-key="@t.Key" class="ui-card" data-cl-form>
                @Html.AntiForgeryToken()
                <input type="hidden" name="Key" value="@t.Key" />

                <div class="ui-card-header">
                    <div>
                        <div class="ui-card-title">@t.FriendlyName</div>
                        <div class="ui-card-muted-body">
                            <span class="ui-mono">@t.Key</span>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary btn-sm">
                        <i class="bx bx-save me-1"></i> @L["Common.Save"]
                    </button>
                </div>
                <div class="ui-card-body">
                    <div class="row g-3">
                        <div class="col-12">
                            <label class="ui-label">@L["NotificationTemplates.Index.SubjectLabel"]</label>
                            <input class="ui-input form-control" name="Subject" value="@t.Subject"
                                   placeholder="@L["NotificationTemplates.Index.SubjectPlaceholder"]" />
                        </div>
                        <div class="col-12">
                            <label class="ui-label">@L["NotificationTemplates.Index.BodyLabel"]</label>
                            <textarea class="ui-input form-control" name="Body" rows="3"
                                      placeholder="@L["NotificationTemplates.Index.BodyPlaceholder"]">@t.Body</textarea>
                            <div class="ui-help-text">
                                @L["NotificationTemplates.Index.TokensLabel"] <code>{customer.name}</code>, <code>{customer.phone}</code>,
                                <code>{order.number}</code>, <code>{order.total}</code>,
                                <code>{order.status}</code>, <code>{wallet.balance}</code>,
                                <code>{wallet.amount}</code>, <code>{tenant.name}</code>.
                            </div>
                        </div>
                    </div>
                </div>
            </form>

            <form method="post" asp-action="TestSend" asp-route-key="@t.Key" class="ui-card" data-cl-form>
                @Html.AntiForgeryToken()
                <input type="hidden" name="Key" value="@t.Key" />
                <div class="ui-card-body">
                    <div class="ui-row" style="gap:.5rem; align-items:flex-end; flex-wrap: wrap">
                        <div>
                            <label class="ui-label">@L["NotificationTemplates.Index.TestChannelLabel"]</label>
                            <select name="Channel" class="form-select ui-select" style="max-width:140px">
                                <option value="Sms">@L["NotificationTemplates.Channel.Sms"]</option>
                                <option value="Email">@L["Common.Email"]</option>
                            </select>
                        </div>
                        <div class="flex-grow-1">
                            <label class="ui-label">@L["NotificationTemplates.TestSend.RecipientLabel"]</label>
                            <input type="text" name="Recipient" class="ui-input form-control"
                                   placeholder="@L["NotificationTemplates.Index.RecipientPlaceholder"]" />
                        </div>
                        <button class="btn btn-outline-secondary btn-sm" type="submit">
                            <i class="bx bx-send me-1"></i> @L["NotificationTemplates.TestSend.SendButton"]
                        </button>
                    </div>
                </div>
            </form>
        }
        </div>
    }
</div>
